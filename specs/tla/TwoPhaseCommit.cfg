\* TLC configuration for TwoPhaseCommit.tla
\*
\* Model checking parameters for 2 transactions and 3 participants.
\* Run with: java -jar tla2tools.jar -config TwoPhaseCommit.cfg TwoPhaseCommit.tla

\* ========================================================================
\* Constants
\* ========================================================================

CONSTANTS
    tx1 = tx1
    tx2 = tx2
    p1 = p1
    p2 = p2
    p3 = p3
    Nil = Nil
    Transactions = {tx1, tx2}
    Participants = {p1, p2, p3}

\* ========================================================================
\* Specification
\* ========================================================================

SPECIFICATION Spec

\* ========================================================================
\* Invariants (Safety Properties)
\* ========================================================================

INVARIANT TypeOK
INVARIANT Atomicity
INVARIANT NoOrphanedLocks
INVARIANT ConsistentDecision

\* ========================================================================
\* Properties (Temporal)
\* ========================================================================

PROPERTY DecisionStability
